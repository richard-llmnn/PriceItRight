{
  "name": "PriceItRight",
  "version": "1.0.0",
  "main": "dist/index.html",
  "repository": "https://github.com/richard-llmnn/PriceItRight",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "format": "prettier --print-width=120  --tab-width=4 --plugin-search-dir=. --write src/",
    "deploy": "npm run build && gh-pages --message \"Deploy version $npm_package_version\" --dist dist"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^2.0.3",
    "gh-pages": "^5.0.0",
    "prettier": "^2.8.7",
    "prettier-plugin-svelte": "^2.10.0",
    "svelte": "^3.57.0",
    "vite": "^4.3.0"
  }
}
